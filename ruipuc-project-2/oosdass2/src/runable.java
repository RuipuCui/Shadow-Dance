import bagel.Input;

public interface runable {

    /**
     * run the level1 game, draw image, print message and
     * calculate the score
     * @param input
     */
    public void startRun(Input input);
}
